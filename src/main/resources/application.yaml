app:
  aws:
    secrets-manager:
      id: ${APP_AWS_SECRETS_MANAGER_ID}
    dynamodb:
      tables:
        champion-configurations: ${APP_AWS_DYNAMODB_TABLES_CHAMPION_CONFIGURATIONS}
        posted-games: ${APP_AWS_DYNAMODB_TABLES_POSTED_GAMES}
